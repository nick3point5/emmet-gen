import { test, describe, expect } from 'vitest'
import { idInstruction } from './idInstruction.js'

describe('idInstruction test', () => {
	test('should exist', () => {
		expect(idInstruction).toBeDefined()
	})
})