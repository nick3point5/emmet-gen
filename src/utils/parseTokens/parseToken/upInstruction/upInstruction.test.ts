import { test, describe, expect } from 'vitest'
import { upInstruction } from './upInstruction.js'

describe('upInstruction test', () => {
	test('should exist', () => {
		expect(upInstruction).toBeDefined()
	})
})