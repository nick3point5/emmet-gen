import { test, describe, expect } from 'vitest'
import { parseToken } from './parseToken.js'

describe('parseToken test', () => {
	test('should exist', () => {
		expect(parseToken).toBeDefined()
	})
})
