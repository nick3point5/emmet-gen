import { test, describe, expect } from 'vitest'
import { classInstruction } from './classInstruction.js'

describe('classInstruction test', () => {
	test('should exist', () => {
		expect(classInstruction).toBeDefined()
	})
})