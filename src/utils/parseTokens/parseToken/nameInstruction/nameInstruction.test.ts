import { test, describe, expect } from 'vitest'
import { nameInstruction } from './nameInstruction.js'

describe('nameInstruction test', () => {
	test('should exist', () => {
		expect(nameInstruction).toBeDefined()
	})
})