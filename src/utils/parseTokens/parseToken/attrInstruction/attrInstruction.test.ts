import { test, describe, expect } from 'vitest'
import { attrInstruction } from './attrInstruction.js'

describe('attrInstruction test', () => {
	test('should exist', () => {
		expect(attrInstruction).toBeDefined()
	})
})
