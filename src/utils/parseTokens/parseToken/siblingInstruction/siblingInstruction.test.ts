import { test, describe, expect } from 'vitest'
import { siblingInstruction } from './siblingInstruction.js'

describe('siblingInstruction test', () => {
	test('should exist', () => {
		expect(siblingInstruction).toBeDefined()
	})
})
