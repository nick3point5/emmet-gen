import { test, describe, expect } from 'vitest'
import { multiplyInstruction } from './multiplyInstruction.js'

describe('multiplyInstruction test', () => {
	test('should exist', () => {
		expect(multiplyInstruction).toBeDefined()
	})
})