import { test, describe, expect } from 'vitest'
import { replaceCountMarker } from './replaceCountMarker.js'

describe('replaceCountMarker test', () => {
	test('should exist', () => {
		expect(replaceCountMarker).toBeDefined()
	})
})
