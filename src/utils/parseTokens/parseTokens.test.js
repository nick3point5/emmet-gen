import { expect } from "vitest"
import { test, describe } from "vitest"
import { parseTokens, Template } from "./parseTokens.js"

const settings = {
	"barrel_imports": false,
	"relative": false,
	"templatesSource": "C:/code/side-bits/em-gen/test/em-gen-templates",
	"baseUrl": "C:/code/side-bits/em-gen/test"
}

describe('Template class', () => {
	test('', () => {
		
	})
})

// describe('parse tokens into templates', () => {

// })